# Backend database settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tienda
DB_USER=posgrest
DB_PASSWORD=123
DB_USE_SQLITE=false

# Uploads directory (use a mounted volume in Docker)
UPLOADS_DIR=/usr/src/app/uploads
